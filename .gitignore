# Admin backend secrets and local data — never commit real credentials or
# draft content to this public repository. See api/auth-config.example.json.
api/auth-config.json
data/checklists.source.json
data/backups/*.json
